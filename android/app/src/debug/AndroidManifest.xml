<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow the existing local HTTP backend only in debug builds. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
